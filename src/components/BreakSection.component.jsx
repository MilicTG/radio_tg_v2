import React from "react";

const BreakSection = () => {
    return (
        <section className="w-full h-80 bg-breakOne bg-fixed bg-center bg-cover">
            <div>Parallax</div>
        </section>
    );
};

export default BreakSection;
